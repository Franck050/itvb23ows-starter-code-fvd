version: '3.0'

services:
  web:
    build: src/PhpWebApp
    ports:
      - "80:80"
    depends_on:
      - db

  db:
    build: src/Database
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: hive